import React from 'react';
import {
  Typography,
} from '@material-ui/core';

const Header = () => (
  <header className="header">
    <Typography
      variant="h1"
      color="primary"
    >
      GOSYP
    </Typography>
  </header>
);
export default Header;
